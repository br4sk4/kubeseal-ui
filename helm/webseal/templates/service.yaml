apiVersion: v1
kind: Service
metadata:
  name: {{ include "webseal.fullname" . }}
  labels:
    {{- include "webseal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webseal.selectorLabels" . | nindent 4 }}
